import 'package:flutter/material.dart';import '../models/alert.dart';import '../services/api_service.dart';class AlertsScreen extends StatelessWidget{const AlertsScreen({super.key,required this.api});final ApiService api;@override Widget build(BuildContext c)=>FutureBuilder<List<AlertInfo>>(future:api.alerts(),builder:(c,s){if(!s.hasData)return Center(child:s.hasError?Text('${s.error}'):const CircularProgressIndicator());return ListView(padding:const EdgeInsets.all(16),children:[for(final a in s.data!)Card(child:ListTile(leading:Icon(a.severity=='critical'?Icons.error:Icons.warning_amber,color:a.severity=='critical'?Colors.red:Colors.amber),title:Text(a.type),subtitle:Text('${a.serverId}
${a.message}')))]);});}
