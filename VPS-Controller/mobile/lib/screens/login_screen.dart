import 'package:flutter/material.dart';class LoginScreen extends StatefulWidget{const LoginScreen({super.key,required this.onLogin});final ValueChanged<String>onLogin;@override State<LoginScreen>createState()=>_S();}class _S extends State<LoginScreen>{final c=TextEditingController(text:'change-me-user');@override void dispose(){c.dispose();super.dispose();}@override Widget build(BuildContext x)=>Scaffold(body:Center(child:ConstrainedBox(constraints:const BoxConstraints(maxWidth:420),child:Padding(padding:const EdgeInsets.all(24),child:Column(mainAxisSize:MainAxisSize.min,children:[const Icon(Icons.dns_rounded,size:64),const SizedBox(height:12),Text('VPS Controller',style:Theme.of(x).textTheme.headlineMedium),const Text('Server Management'),const SizedBox(height:28),TextField(controller:c,decoration:const InputDecoration(labelText:'Token do usuário',border:OutlineInputBorder())),const SizedBox(height:16),FilledButton(onPressed:()=>widget.onLogin(c.text.trim()),child:const Text('Entrar'))])))));}
