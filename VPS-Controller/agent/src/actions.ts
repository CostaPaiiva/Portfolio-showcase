import { api } from './api.js';import { config } from './config.js';import { dockerAction } from './docker.js';import { serviceAction } from './systemd.js';
type A={id:string;type:'docker.start'|'docker.stop'|'docker.restart'|'service.start'|'service.stop'|'service.restart';target:string};
export async function actions(){const x=await api(`/api/agent/actions?serverId=${encodeURIComponent(config.serverId)}`) as {actions:A[]};for(const a of x.actions){let success=false,result='';try{const [family,op]=a.type.split('.') as ['docker'|'service','start'|'stop'|'restart'];result=family==='docker'?await dockerAction(op,a.target):await serviceAction(op,a.target);success=true;}catch(e){result=e instanceof Error?e.message:String(e);}await api(`/api/agent/actions/${encodeURIComponent(a.id)}/result`,{method:'POST',body:JSON.stringify({success,result})});}}
